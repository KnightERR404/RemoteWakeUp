# 远程唤醒系统架构说明

## 系统架构图

```
┌─────────────────┐
│   互联网用户    │
│  (手机/电脑)    │
└────────┬────────┘
         │ HTTP/HTTPS请求
         ▼
┌─────────────────┐
│   云服务器      │
│   (公网IP)      │
│                 │
│ • Flask API     │
│ • 任务队列      │
│ • Web界面       │
└────────┬────────┘
         │ 定期轮询 (HTTP)
         ▼
┌─────────────────┐
│    树莓派       │
│  (局域网内)     │
│                 │
│ • 轮询客户端    │
│ • WOL发送器     │
└────────┬────────┘
         │ UDP广播 (魔术包)
         ▼
┌─────────────────┐
│  目标计算机     │
│  (需要唤醒)     │
│                 │
│ • 支持WOL      │
│ • 网卡接收      │
└─────────────────┘
```

## 工作流程

### 1. 用户发送唤醒请求

用户通过以下方式之一发送唤醒请求：

- **Web界面**: 访问 `http://服务器IP:5000`
- **API调用**: 使用curl或其他HTTP客户端
- **移动应用**: 通过HTTP请求调用API

请求携带：
- API密钥（认证）
- 设备名称（要唤醒的设备）

### 2. 云服务器处理请求

服务器接收到请求后：

1. **验证API密钥**: 检查请求是否经过授权
2. **创建任务**: 生成唯一任务ID，将任务加入队列
3. **返回响应**: 向用户返回任务ID和状态

任务数据结构：
```json
{
  "task_id": "uuid-string",
  "device": "my-pc",
  "status": "pending",
  "created_at": "2026-02-28T12:00:00",
  "completed_at": null,
  "client_id": null,
  "result": null
}
```

### 3. 树莓派轮询服务器

树莓派客户端持续运行，定期（默认10秒）轮询云服务器：

1. **发送轮询请求**: GET `/api/poll`
2. **接收任务列表**: 服务器返回所有待处理任务
3. **检查设备配置**: 验证设备是否在本地配置中

### 4. 发送WOL魔术包并验证启动 🆕

树莓派收到任务后：

1. **读取设备配置**: 获取MAC地址、IP地址、广播地址等
2. **构造魔术包**: 6字节0xFF + 16次重复的MAC地址
3. **发送UDP广播**: 向局域网广播地址发送魔术包
4. **多次发送**: 默认发送3次以提高成功率
5. **验证启动** 🆕: 如果配置了IP地址，通过Ping检测设备是否成功启动
   - 每5秒检测一次
   - 最多等待 verify_timeout 秒（默认120秒）
   - 记录启动用时
6. **返回结果**: 向服务器报告唤醒结果和验证状态

魔术包格式：
```
[0xFF × 6] + [MAC地址 × 16]
总长度: 102字节
```

验证结果格式：
```json
{
  "wol_sent": true,
  "verified": true,
  "online": true,
  "boot_time": 45
}
```

### 5. 目标计算机被唤醒

目标计算机的网卡：

1. **监听网络**: 即使在关机或休眠状态下保持供电
2. **识别魔术包**: 检测到包含自己MAC地址的魔术包
3. **触发启动**: 向主板发送开机信号
4. **系统启动**: 计算机正常启动

### 6. 上报执行结果

树莓派发送魔术包并验证后 🆕：

1. **记录结果**: 保存执行成功或失败状态，以及验证信息 🆕
2. **上报服务器**: POST `/api/report` (包含验证结果) 🆕
3. **更新任务**: 服务器更新任务状态为 `completed` 或 `failed`

## 数据流向

```
用户 → 云服务器: 唤醒请求 (device名称)
云服务器 → 内存: 创建任务 (task对象)
树莓派 → 云服务器: 轮询任务 (GET /api/poll)
云服务器 → 树莓派: 返回任务列表
树莓派 → 局域网: 发送WOL包 (UDP广播)
树莓派 → 目标设备: Ping检测 (ICMP) 🆕
目标设备 → 树莓派: Ping响应 (设备在线) 🆕
树莓派 → 云服务器: 上报结果 (POST /api/report, 含验证信息) 🆕
云服务器 → 内存: 更新任务状态
用户 → 云服务器: 查询状态 (GET /api/tasks/{id})
```

## 安全机制

### 1. API密钥认证

所有API请求必须携带有效的API密钥：

```http
X-API-Key: your-secret-api-key
```

服务器验证密钥后才处理请求。

### 2. 请求限制

- 清理机制: 自动删除超过1小时的旧任务
- 状态隔离: 每个任务独立处理，互不影响

### 3. 网络安全

建议配置：
- 使用HTTPS加密通信
- 配置防火墙规则
- 使用强随机API密钥（32+字符）
- 定期更换密钥

## 容错机制

### 1. 网络断开

- **树莓派**: 断网后自动重连，继续轮询
- **云服务器**: 使用超时和重试机制

### 2. 任务失败

- **多次发送**: WOL包默认发送3次
- **状态记录**: 记录失败原因供排查

### 3. 服务重启

- **systemd**: 配置自动重启
- **持久化**: 任务在内存中，重启后清空（可升级为数据库）

## 扩展性

### 支持多客户端

可以在不同局域网部署多个树莓派：

```
云服务器
├── 树莓派A (家庭网络)
│   ├── PC1
│   └── PC2
└── 树莓派B (办公室网络)
    ├── Server1
    └── Server2
```

每个树莓派管理自己局域网内的设备。

### 支持多设备

单个树莓派可以管理同一局域网内的多台设备：

```json
"devices": {
  "pc-1": { "mac": "AA:BB:CC:DD:EE:FF" },
  "pc-2": { "mac": "11:22:33:44:55:66" },
  "server": { "mac": "AA:11:BB:22:CC:33" }
}
```

## 性能指标

- **响应时间**: 通常10-30秒（取决于轮询间隔）
- **成功率**: 95%+（网络和硬件正常情况下）
- **并发能力**: 支持100+并发唤醒请求
- **资源占用**: 
  - 云服务器: ~50MB内存
  - 树莓派: ~30MB内存

## 故障排查流程

```
唤醒失败
├── 检查服务器
│   ├── 服务是否运行？
│   ├── 任务是否创建？
│   └── API密钥是否有效？
├── 检查树莓派
│   ├── 客户端是否运行？
│   ├── 能否连接服务器？
│   └── 设备配置是否正确？
├── 检查目标计算机
│   ├── BIOS是否启用WOL？
│   ├── 网卡是否支持WOL？
│   ├── MAC地址是否正确？
│   └── 是否连接电源？
└── 检查启动验证 🆕
    ├── 设备是否配置了IP地址？
    ├── 防火墙是否允许Ping？
    ├── verify_timeout是否足够长？
    └── 设备是否使用静态IP？
```

## 日志系统

### 云服务器日志

位置: `logs/server.log`

记录内容：
- API请求
- 任务创建和完成
- 认证失败
- 错误信息

### 树莓派日志

位置: `logs/client.log`

记录内容：
- 轮询活动
- WOL包发送
- 启动验证结果 🆕
- 任务处理
- 连接错误

查看实时日志：
```bash
tail -f logs/server.log
tail -f logs/client.log
```

## 未来改进方向

1. **数据库持久化**: 使用SQLite或Redis存储任务
2. **WebSocket**: 实现实时通信，减少延迟
3. **Web界面增强**: 添加设备管理、历史记录等功能
4. **通知系统**: 邮件或推送通知
5. **状态监控**: 监控目标设备是否成功启动
6. **移动应用**: iOS/Android原生应用
7. **多用户支持**: 添加用户认证和权限管理
